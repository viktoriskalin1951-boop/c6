# Браузерный калькулятор

Простой калькулятор, реализованный на чистом HTML, CSS и JavaScript.

## 🚀 Демо

После деплоя на Vercel, калькулятор будет доступен по вашему домену.

## ✨ Возможности

- Базовые арифметические операции: сложение, вычитание, умножение, деление
- Поддержка процентов
- Удаление последнего символа (backspace)
- Адаптивный дизайн для мобильных устройств
- Обработка деления на ноль

## 📁 Структура проекта

```
├── index.html      # Главная страница (калькулятор)
├── calculator.html # Альтернативная версия калькулятора
├── vercel.json     # Конфигурация для Vercel
├── .gitignore      # Игнорируемые файлы Git
├── LICENSE         # Лицензия
└── README.md       # Документация
```

## 🌐 Деплой на Vercel

### Способ 1: Через GitHub (рекомендуется)

1. Загрузите проект на GitHub (если ещё не загружен)
2. Перейдите на [vercel.com](https://vercel.com) и войдите в аккаунт
3. Нажмите **"Add New..."** → **"Project"**
4. Выберите репозиторий `c6` из списка
5. Нажмите **"Deploy"**

Vercel автоматически определит, что это статический сайт, и настроит деплой.

### Способ 2: Через Vercel CLI

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. Войдите в аккаунт:
   ```bash
   vercel login
   ```

3. Запустите деплой из папки проекта:
   ```bash
   vercel
   ```

4. Для продакшн-деплоя:
   ```bash
   vercel --prod
   ```

## 🛠 Локальный запуск

Просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
# С помощью Python
python -m http.server 8000

# С помощью Node.js (npx)
npx serve .
```

Затем откройте http://localhost:8000 в браузере.

## 📄 Лицензия

Смотрите файл [LICENSE](LICENSE).
